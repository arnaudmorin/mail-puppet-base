#!/bin/bash

source /usr/local/status/lib/status
[[ $? -ne 0 ]] && exit 1


function check
{
    status.log "[INFO] Checking if php7.0-fpm is running"
    systemctl --quiet is-active php7.0-fpm && status.set $STATUS_OK || status.set $STATUS_ERROR
}

status.run check
