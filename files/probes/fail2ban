#!/bin/bash

source /usr/local/status/lib/status
[[ $? -ne 0 ]] && exit 1


function check
{
    status.log "[INFO] Checking if fail2ban is running"
    systemctl --quiet is-active fail2ban && status.set $STATUS_OK || status.set $STATUS_ERROR
}

status.run check
